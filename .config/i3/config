# This file has been auto-generated by i3-config-wizard(1).

#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# #---Basic---# #
gaps inner 3
gaps outer 0
default_border pixel 3

# class                 border  backgr. text    indicator child_border
# client.focused          #4c7899 #285577 #ffffff #2e9ef4   #d79921
client.focused          #b8bb26 #8ec07c #ffffff #b16286   #d79921

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Dual monitor setup
bindsym $mod+F8 exec --no-startup-id ~/.config/i3/sel_monitor
workspace "1" output eDP1 
workspace "2" output eDP1 
workspace "3" output eDP1 
workspace "4" output eDP1 
workspace "5" output eDP1 
workspace "6" output eDP1 
workspace "7" output eDP1 
workspace "8" output eDP1 

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10


# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"
# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
# bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Return exec --no-startup-id st
# start dmenu (a program launcher)
bindsym $mod+Shift+p exec --no-startup-id dmenu_run -fn "DajaVu Sans Mono:size=14"
bindsym $mod+p exec --no-startup-id dmenu_run2 -fn "DajaVu Sans Mono:size=14"
# Luke's dmemu emoji
bindsym $mod+grave exec --no-startup-id ~/.config/i3/emoji/dmenuunicode

# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# kill focused window
bindsym $mod+q kill
# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
# move focused window
bindsym $mod+Shift+h move left 53px
bindsym $mod+Shift+j move down 53px
bindsym $mod+Shift+k move up 53px
bindsym $mod+Shift+l move right 53px
# >>> Dwindle >>>
default_orientation vertical
for_window [class=".*"] split toggle # toggles split at each new window
# split in horizontal orientation
# bindsym $mod+b split h
bindsym $mod+bracketright split h
# split in vertical orientation
# bindsym $mod+v split v
bindsym $mod+bracketleft split v
# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle
# toggle tiling / floating
bindsym $mod+Shift+space floating toggle
bindsym $mod+y sticky toggle
# Luke's resizing script
bindsym $mod+Shift+y exec --no-startup-id ~/.config/i3/i3resize left
bindsym $mod+Shift+u exec --no-startup-id ~/.config/i3/i3resize down
bindsym $mod+Shift+i exec --no-startup-id ~/.config/i3/i3resize up
bindsym $mod+Shift+o exec --no-startup-id ~/.config/i3/i3resize right 
# #--- Fn keys ---# #
# Backlight xbacklight
# Fn+F11
bindcode 232 exec --no-startup-id bash ~/.config/i3/brightness/brightness_down
# Fn+F12
bindcode 233 exec --no-startup-id bash ~/.config/i3/brightness/brightness_up
# Sound amixer
# Fn+F2
# bindcode 122 exec --no-startup-id amixer sset Master 5%-
bindcode 122 exec --no-startup-id bash ~/.config/i3/volume/volume_down
# Fn+F3
# bindcode 123 exec --no-startup-id amixer sset Master 5%+
bindcode 123 exec --no-startup-id bash ~/.config/i3/volume/volume_up 
bindcode 121 exec --no-startup-id bash ~/.config/i3/volume/audio_toggle
# graphicsmagick
# for screen capture and snipping
bindsym Print exec --no-startup-id ~/.config/i3/graphicsmagick/gm-capture
bindsym $mod+Print exec --no-startup-id ~/.config/i3/graphicsmagick/gm-snipping

# polybar
exec_always --no-startup-id $HOME/.config/polybar/launch.sh
# Start nm-applet
exec --no-startup-id nm-applet


# >>> Dropdown/Scratchpad windows >>>
# First I have a tmux window used for background scripts.
# I'll later bind this to mod+u
# bindsym $mod+plus [title="__focused__"] resize grow up 5px;[title="__focused__"] resize grow down 5px;[title="__focused__"] resize grow left 8px;[title="__focused__"] resize grow right 8px;    
# bindsym $mod+minus [title="__focused__"] resize shrink up 5px;[title="__focused__"] resize shrink down 5px;[title="__focused__"] resize shrink left 8px;[title="__focused__"] resize shrink right 8px;    

# move __focused__
bindsym $mod+Left  [title="__focused__"] move left 31px
bindsym $mod+Down  [title="__focused__"] move down 31px
bindsym $mod+Up    [title="__focused__"] move up 31px
bindsym $mod+Right [title="__focused__"] move right 31px

	# >>> dropdown_term >>>
	for_window [instance="dropdown_term"] floating enable
	for_window [instance="dropdown_term"] move scratchpad
	for_window [instance="dropdown_term"] sticky enable
	for_window [instance="dropdown_term"] resize set 742 530 #14:10 * 53 #tput cols=81
	for_window [instance="dropdown_term"] border pixel 3
	for_window [instance="dropdown_term"] move position center
	bindsym $mod+u exec --no-startup-id ~/.config/i3/ddspawn term

	for_window [instance="dropdown_lf"] floating enable
	for_window [instance="dropdown_lf"] move scratchpad
	for_window [instance="dropdown_lf"] sticky enable
	for_window [instance="dropdown_lf"] resize set 1113 530
	for_window [instance="dropdown_lf"] border pixel 5
	for_window [instance="dropdown_lf"] move position center
	bindsym $mod+r exec --no-startup-id ~/.config/i3/ddspawn lf
	# bindsym $mod+u [title="dropdown_term"] scratchpad show; [title="dropdown_term"] move position center
	# exec --no-startup-id st -t "dropdown_term"
	# <<< dropdown_term <<<

	# >>> Slack >>>
	for_window [instance="slack"] floating enable
	for_window [instance="slack"] move scratchpad
	for_window [instance="slack"] scratchpad show
	for_window [instance="slack"] sticky enable
	for_window [instance="slack"] resize set 1280 720 
	for_window [instance="slack"] move position center
	bindsym $mod+s [instance="slack"] scratchpad show; [instance="slack"] move position center
	# <<< Slack <<<
	
	# >>> Spotify >>>
	for_window [instance="spotify"] floating enable
	for_window [instance="spotify"] move scratchpad
	for_window [instance="spotify"] scratchpad show
	for_window [instance="spotify"] sticky enable
	for_window [instance="spotify"] resize set 1280 720
	for_window [instance="spotify"] move position center
	bindsym $mod+m [instance="spotify"] scratchpad show; [instance="spotify"] move position center
# <<< Dropdown/Scratchpad windows <<<

# >>> Inkscape >>>
for_window [class="Inkscape"] floating enable
# >>> Zoom >>>
for_window [class="zoom"] floating enable

# #--- Not using ---# #
# change container layout (stacked, tabbed, toggle split)
# bindsym $mod+s layout stacking
# bindsym $mod+w layout tabbed
# bindsym $mod+e layout toggle split

# change focus between tiling / floating windows
# bindsym $mod+space focus mode_toggle

# focus the parent container
# bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

## i3bar
# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
# bar {
#         status_command i3blocks
# 	mode dock
# 	position bottom 
# 	tray_output primary
# 	font pango:Noto Sans Mono Regular 11 
# 	# separator_symbol ":|:"
# 	colors {
# 		# <colorclass> <border> <background> <text>
# 		background #000000
# 		# statusline #ffffff
# 		# separator
# 		# orange d79921 defualt_border 4c7899 285577 
# 		focused_workspace    #be9543 #d79921 #5b21d7
#         	# active_workspace   #333333 #5f676a #ffffff
# 	        # inactive_workspace #333333 #222222 #888888
# 	        # urgent_workspace   #2f343a #900000 #ffffff
# 	        # binding_mode       #2f343a #900000 #ffffff
# 
# 	}
#   
# # i3 bar config
# 	# disable clicking on workspace buttons
# 	bindsym button1 nop
# }
