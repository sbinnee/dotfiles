#!/bin/bash

c=$(echo -e "yes\nno" | dmenu -fn "Nono Sans Mono-13" -p "Screen snipping?")

if [ $c = 'yes' ]; then
	f=$(date '+%Y%m%d-%H%M%S.png')
	gm import ~/Pictures/$f
	notify-send -t 2800 "Screen snipped" "~/Pictures/$f"
else
	exit
fi
