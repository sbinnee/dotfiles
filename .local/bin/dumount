#!/bin/sh
drives=$(lsblk -nrpo "name,type,size,mountpoint" | awk '$4!~/\/boot|\/home$|SWAP|^\/mnt\/storage/&&length($4)>1{printf "%s (%s)\n",$4,$3}')
remotedrives=$(rclone listremotes | tr -d ':')
sambas="cassis [LOB]\nmorphoscope [LOB]"
# echo $drives
remotes=$(df -h | grep -P ':|//' | awk '{printf "%s (%s)\n",$6,$2}')
# remotes=$(df -h | grep -P ':|//' | awk '{printf "%s (%s) [rclone]\n",$6,$2}')

# Check if any
[ -z "$drives$remotes" ] && notify-send "💻 No drives to unmount" && exit
chosen="$(echo -e "$drives\n$remotes" | dmenu -l 10 -i -p "Unmount which drive?")" || exit 1
chosen="$(echo "$chosen" | awk '{print $1}')"

if [[ "$remotedrives" = *"${chosen##*/}"* ]]
then
	echo "Remote selected"
	fusermount -u "$chosen" && \
		notify-send -i "$HOME/.config/dunst/rclone_logo.png" -t 10000 "Unmounting remote" "$chosen" || \
		notify-send -i "$HOME/.config/dunst/rclone_logo.png" -u ciritical -t 5000 "Failed unmounting"
elif [[ "$sambas" = *"${chosen##*/}"* ]]
then
	sudo -A umount "$chosen" && \
		notify-send "💻 Samba unmounting" "$chosen unmounted." || \
		notify-send -u ciritical -t 5000 "💻 Failed Samba unmounting" "$chosen"
	echo "Sambas" "$chosen"
else
	echo "Local selected"
	sudo -A umount "$chosen" && \
		notify-send "💻 USB unmounting" "$chosen unmounted." || \
		notify-send -u ciritical -t 5000 "💻 Failed USB unmounting" "$chosen"
fi

# [ -z "$drives" ] && echo "No drives to unmount." &&  exit

