#!/bin/sh
# Wrapper program for newsboat
# Update polybar right after exit
if [ -n "$(ps $(pidof firefox) | grep 'firefox -P sb')" ]
then
	export BROWSER=firefox_sb
fi
newsboat && polybar-msg hook newsboat 1
