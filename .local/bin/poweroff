#!/bin/sh
#: Description : Overwrite defulat poweroff and give a nice dmenu prompt to
# 				avoid accidental execution
if [ -n "$(pgrep 'Xorg')" ]
then
	ans=$(echo -e "no\nYes" | dmenu -i -p "Poweroff? [n/Y]")
	[ "$ans" = "Yes" ] && /usr/bin/poweroff
else
	/usr/bin/poweroff
fi
