#!/bin/sh
# Volume down using $mod+F2

val=$(amixer sget Master | grep -oP '\[\d+' -m 1 | tr -d '[')
# echo $val
# #--- amixer (ALSA) ---# #
if [ "$val" -le 20 ]; then
	amixer sset Master 2%-;
else
	amixer sset Master 5%-;
fi
# #--- pulseaudio ---# #
# pactl set-sink-volume 0 -5%
msg=$(amixer sget Master | grep -oP '\[\d+' -m 1 | tr -d '[')
notify-send -u low -t 700 "🔉 $msg"
